#!/bin/bash
# Monitors the number of models processed
run/make_piecewise_predictions/collate
 COUNT=`grep model data/piecewise_predictions.csv | sed 's/,/  /g' | awk '{print $1}' | uniq | wc | awk '{print $1}'`
 echo "Number of models processed: $COUNT"
